const state = {
  currentSelect: null
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  namespaced: false,
  state,
  getters,
  actions,
  mutations
}
